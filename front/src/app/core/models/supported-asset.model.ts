export interface BistSymbol {
  ticker: string;
  companyName: string;
  sector: string;
}

export interface CryptoSymbol {
  symbol: string;
  baseAsset: string;
  name: string;
}

export interface PreciousMetalSymbol {
  symbol: string;
  name: string;
}
